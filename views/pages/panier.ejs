<!DOCTYPE html>
<html>
<head>
	<title>Mon panier</title>
	<style>
		body {
			display: flex;
			flex-direction: row;
			margin: 0;
			padding: 0;
			font-family: Arial, sans-serif;
			font-size: 16px;
			color: #333;
		}

		#left-side {
			flex: 2;
			background-color: #f1f1f1;
			padding: 20px;
		}

		#right-side {
			flex: 1;
			background-color: #ddd;
			padding: 20px;
			text-align: center;
		}

		table {
			width: 100%;
			border-collapse: collapse;
			margin-bottom: 20px;
		}

		th, td {
			padding: 10px;
			text-align: left;
			border-bottom: 1px solid #ddd;
		}

		th {
			background-color: #ccc;
		}

		tr:nth-child(even) {
			background-color: #f2f2f2;
		}

		#total-price {
			font-size: 24px;
			font-weight: bold;
			margin-top: 20px;
		}
	</style>
</head>
<body>
	<div id="left-side">
		<h1>Mon panier</h1>
		<table>
			<thead>
				<tr>
					<th>Nom</th>
					<th>Taille</th>
					<th>Prix unitaire</th>
				</tr>
			</thead>
			<tbody>
				<% var total = 0; %>
				<% for (var i = 0; i < vetements.length; i++) { %>
				<% total += parseFloat(vetements[i].prix) %>
				<tr>
					<td><%= vetements[i].nom %></td>
					<td><%= vetements[i].taille %></td>
					<td><%= vetements[i].prix %> €</td>
				</tr>
				<% } %>
			</tbody>
		</table>
	</div>
	<div id="right-side">
		<h2>Total</h2>
		<div id="total-price">
			<h1><%= total %></h1>
		</div>
	</div>
</body>
</html>
